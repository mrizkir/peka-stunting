# Salin ke `.env.testing` sebelum menjalankan test:
#   cp .env.testing.example .env.testing
# Lalu salin APP_KEY dan DB_PASSWORD dari `.env` jika perlu.

APP_KEY=

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3307
DB_DATABASE=pekastunting_testing
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=array
CACHE_STORE=array
QUEUE_CONNECTION=sync
MAIL_MAILER=array
